name: 🔐 DevSecOps - Full Security Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  # 1️⃣ SAST - Analyse du code source
  sast:
    name: 🔍 SAST - Semgrep Static Analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Semgrep
        run: pip install semgrep
      - name: Run Semgrep
        run: |
          semgrep --config=.semgrep/java-password-sql.yml --json -o semgrep-report.json || true
          semgrep --config=.semgrep/java-password-sql.yml --text -o semgrep-report.txt || true
      - name: ⛔ Fail if Semgrep found issues
        run: |
          if grep -q '"check_id"' semgrep-report.json; then
            echo "❌ Vulnérabilités détectées par Semgrep !"
            exit 1
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: semgrep-report
          path: |
            semgrep-report.json
            semgrep-report.txt

  # 2️⃣ SCA - Analyse des dépendances
  sca:
    name: 🧩 SCA - OWASP Dependency Check
    runs-on: ubuntu-latest
    needs: sast
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
      - name: Install OWASP Dependency-Check
        run: |
          wget https://github.com/jeremylong/DependencyCheck/releases/download/v10.0.3/dependency-check-10.0.3-release.zip
          unzip dependency-check-10.0.3-release.zip
          mv dependency-check /opt/dependency-check
          chmod +x /opt/dependency-check/bin/dependency-check.sh
      - name: Run Dependency Check
        run: |
          /opt/dependency-check/bin/dependency-check.sh \
            --project "DevSecOps-Demo" \
            --scan ./ \
            --format "ALL" \
            --out dependency-report || true
      - name: ⛔ Fail if critical vulnerabilities
        run: |
          if grep -q "CRITICAL" dependency-report/dependency-check-report.html; then
            echo "❌ Vulnérabilités critiques détectées !"
            exit 1
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: dependency-report
          path: dependency-report

  # 3️⃣ Docker Scan - Image container
  docker_scan:
    name: 🐳 Docker Image Scan - Trivy
    runs-on: ubuntu-latest
    needs: sca
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build Docker image
        run: docker build -t devsecops-demo:latest .
      - name: Run Trivy Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: "devsecops-demo:latest"
          format: "json"
          output: "docker-scan.json"
          exit-code: "0"
      - name: ⛔ Fail if critical vulnerabilities
        run: |
          if grep -q "CRITICAL" docker-scan.json; then
            echo "❌ Vulnérabilités critiques dans l’image Docker !"
            exit 1
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: docker-scan-report
          path: docker-scan.json

  # 4️⃣ DAST - Test dynamique
  dast:
    name: 🌐 DAST - OWASP ZAP
    runs-on: ubuntu-latest
    needs: docker_scan
    steps:
      - uses: actions/checkout@v4
      - name: Run OWASP ZAP baseline scan
        uses: zaproxy/action-baseline@v0.11.0
        with:
          target: "http://testphp.vulnweb.com"
          cmd_options: "-a -T 5"
      - uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: |
            report_html.html
            report_md.md
            report_json.json

  # 5️⃣ Secrets - Gitleaks
  secrets:
    name: 🔒 Secrets Scan - Gitleaks
    runs-on: ubuntu-latest
    needs: dast
    steps:
      - uses: actions/checkout@v4
      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        with:
          args: detect --source . --report-format json --report-path gitleaks-report.json || true
      - name: ⛔ Fail if secrets found
        run: |
          if grep -q '"Description"' gitleaks-report.json; then
            echo "❌ Secrets détectés dans le dépôt !"
            exit 1
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: gitleaks-report
          path: gitleaks-report.json

  # 6️⃣ SonarQube Scan (optionnel, non-bloquant)
  sonar:
    name: 🔍 SonarQube Scan
    runs-on: ubuntu-latest
    needs: [sast, sca, docker_scan, dast, secrets]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run SonarQube analysis
        uses: SonarSource/sonarqube-scan-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          projectBaseDir: .
          args: >
            -Dsonar.projectKey=SonarQube-GITHUB
            -Dsonar.projectName=SonarQube-GITHUB

      - name: End SonarQube Scan
        run: echo "SonarQube scan terminé (résultats visibles dans ton instance)."
