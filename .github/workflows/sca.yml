name: 🔍 SCA - Analyse des dépendances avec Trivy

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  sca:
    name: Analyse des dépendances (Trivy)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      # ✅ Installation propre de Trivy depuis GitHub (fonctionne sur Ubuntu 24.04)
      - name: Installer Trivy (binaire officiel)
        run: |
          echo "📦 Téléchargement et installation de Trivy..."
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh
          trivy --version

      # 🔍 Lancer l'analyse des dépendances
      - name: Lancer l'analyse des dépendances avec Trivy
        run: |
          echo "🚀 Démarrage de l'analyse SCA avec Trivy..."
          trivy fs --scanners vuln,license . > sca-report.txt || true
          echo "✅ Rapport généré : sca-report.txt"

      # 📤 Upload du rapport comme artefact
      - name: Upload du rapport SCA
        uses: actions/upload-artifact@v4
        with:
          name: sca-report
          path: sca-report.txt
